# 分类CSV生成脚本所需的Python依赖包

# 核心依赖
pandas>=1.3.0          # 数据处理和CSV生成
tqdm>=4.62.0           # 进度条显示

# robocrystallographer依赖
spglib>=2.5.0
numpy
scipy
pymatgen>=2024.1.26
inflect
networkx
matminer>=0.9.2
monty
pubchempy
pybtex

# 可选依赖（用于更好的分子名称识别）
# openbabel  # 需要使用 conda install -c conda-forge openbabel 安装
